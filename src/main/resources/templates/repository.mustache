{{! allcrud-generator: brand-new template, NOT an override of an embedded openapi-generator
    file - the stock "spring" generator has no repository template. Registered as a per-tag
    "api template file" via CodegenConfig#apiTemplateFiles() in the generation driver code
    (see AllcrudGenerator), with output suffix "Repository.java".

    allcrudEntityName / allcrudIdType / allcrudEntityPackage are resolved by
    AllcrudSpringCodegen (a custom CodegenConfig, not the stock "spring" one) by inspecting
    the actual OpenAPI model and, for the entity package, scanning the source tree - see that
    class for the resolution logic.
    Generated class name is prefixed with allcrudEntityName ("Product"), not the stock
    per-tag classname variable ("ProductsApi", plural, "Api"-suffixed) - that mismatch was
    a naming bug fixed after the fact, not an intentional customization. }}
package {{package}};

import com.techmath.allcrud.repository.EntityRepository;
import {{allcrudEntityPackage}}.{{allcrudEntityName}};

{{>generatedAnnotation}}
{{#operations}}
public interface {{allcrudEntityName}}Repository extends EntityRepository<{{allcrudEntityName}}, {{allcrudIdType}}> {
}
{{/operations}}
