{{! allcrud-generator: brand-new template, NOT an override of an embedded openapi-generator
    file (and NOT related to the stock "converter.mustache" that ships with the spring
    generator, which is Spring's enum string-conversion beans - unrelated concept, same
    filename only by coincidence; this one replaces it because it's registered under the
    same name via CodegenConfig#apiTemplateFiles() in the generation driver code, see
    AllcrudGenerator, with output suffix "Converter.java").

    Generates a skeleton implementing com.techmath.allcrud.converter.Converter, with both
    methods throwing UnsupportedOperationException. This is a new pattern established by
    this generator, not based on any existing core precedent - it exists so the generated
    Controller/Service have something to compile and wire against out of the box, while
    making it obvious and loud that the actual entity<->VO mapping still needs to be
    written by hand.

    allcrudEntityName / allcrudPojoClassName / allcrudIdType / allcrudEntityPackage /
    allcrudPojoPackage are resolved by AllcrudSpringCodegen - see that class. Entity/POJO
    imports use those resolved packages, not hardcoded "org.openapitools.*" - each of the 5
    layers can live in a different package (allcrud-generator.yml's "packages" section).
    Generated class name is prefixed with allcrudEntityName ("Product"), not the stock
    per-tag classname variable ("ProductsApi", plural, "Api"-suffixed) - that mismatch was
    a naming bug fixed after the fact, not an intentional customization. }}
package {{package}};

import com.techmath.allcrud.converter.Converter;
import {{allcrudEntityPackage}}.{{allcrudEntityName}};
import {{allcrudPojoPackage}}.{{allcrudPojoClassName}};

{{>generatedAnnotation}}
{{#operations}}
public class {{allcrudEntityName}}Converter implements Converter<{{allcrudEntityName}}, {{allcrudPojoClassName}}, {{allcrudIdType}}> {

    @Override
    public {{allcrudPojoClassName}} convertToVO({{allcrudEntityName}} entity) {
        throw new UnsupportedOperationException("Override this method in {{allcrudEntityName}}Converter to define the entity-to-VO mapping");
    }

    @Override
    public {{allcrudEntityName}} convertToEntity({{allcrudPojoClassName}} vo) {
        throw new UnsupportedOperationException("Override this method in {{allcrudEntityName}}Converter to define the VO-to-entity mapping");
    }

}
{{/operations}}
